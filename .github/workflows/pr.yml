name: hello-world
on:
  pull_request:
    types: [ labeled ]
jobs:
  tests:
    if: ${{ github.event.label.name == 'run' }}
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"